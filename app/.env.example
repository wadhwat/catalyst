# Backend API base URL. Copy this file to .env before running the app.
# For physical phone testing: use your machine's LAN IP or public backend URL
# (e.g. http://192.168.1.100:8000). localhost only works in the emulator.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000
